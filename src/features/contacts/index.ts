// Public API - only export components used externally
export * from "./components/ContactList";
export * from "./types";

// Internal components (not exported):
// - ContactCard (only used within ContactList)
